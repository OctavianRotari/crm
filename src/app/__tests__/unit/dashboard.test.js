import React from 'react';
import { shallow } from 'enzyme';

import Dashboard from '../../scenes/dashboard';

describe('<Dashboard />', () => {
    it('dummy test', () => {
        expect(1 + 2).toBe(3);
    });
});
