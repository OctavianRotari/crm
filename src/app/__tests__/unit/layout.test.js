import React from 'react';
import { shallow } from 'enzyme';

import Layout from '../../Layout';

describe('<Layout />', () => {
    it('dummy test', () => {
        expect(1 + 2).toBe(3);
    });
});
